#include "SaveScreen.hpp"
#include "Interactable.hpp"

using lif::ui::SaveScreen;

SaveScreen::SaveScreen(const sf::RenderWindow& window, const sf::Vector2u& size)
	: lif::ui::Screen(window, size)
{}
