#pragma once

#include <SFML/System/Time.hpp>

namespace lif {

namespace conf {
	const sf::Time EXTRA_GAME_DURATION = sf::seconds(30);
	/** Duration of the hurt effect */
	const sf::Time HURT_TIME = sf::seconds(0.4f);
}

}
